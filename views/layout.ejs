<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= typeof title !== 'undefined' ? `${title} | `: '' %>IP Location API</title>
    <link rel="icon" href="/favicon.ico">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/stylesheets/normalize.css">
    <link rel="stylesheet" href="/stylesheets/fontawesome.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body>
    <%- body %>
    <script>
      function events() {
        document.addEventListener('click', event => {
          const element = event.target;
          element.matches('.copy-button.api-my-ip') ? copyText('https://autumnchris-ip-location-api.onrender.com/api/ip/me') : null;
          element.matches('.copy-button.api-custom-ip') ? copyText('https://autumnchris-ip-location-api.onrender.com/api/ip/72.14.192.0') : null;
        });
      }

      function copyText(text) {
        navigator.clipboard.writeText(text);
      }
      
      events();
    </script>
  </body>
</html>